## 1. OpenSpec initialization

- [x] 1.1 Generate the repository OpenSpec structure with the `spec-driven` schema and verify `openspec/config.yaml` and directory markers are present
- [x] 1.2 Generate the shared `.agents/skills` instructions and verify all seven OpenSpec workflow skills are present

## 2. Capability and quality validation

- [x] 2.1 Add the `openspec-initialization` capability specification and verify it contains normative requirements with scenarios
- [x] 2.2 Run strict OpenSpec and shared-skill validation, then verify the completed change can be archived
